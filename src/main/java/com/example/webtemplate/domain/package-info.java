/**
 * Business features live here. Create one package per domain, such as user,
 * product, order, or board, and place its MVC layers inside that package.
 */
package com.example.webtemplate.domain;
